import { DialogLineTransition } from './dialog-line-transition';

describe('DialogLineTransition', () => {
  it('should create an instance', () => {
    expect(new DialogLineTransition()).toBeTruthy();
  });
});
